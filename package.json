{
  "name": "halfspacefc-site",
  "private": true,
  "version": "1.0.0",
  "description": "Build and validation pipeline for the Half Space static site",
  "scripts": {
    "build": "node tools/build-site.mjs",
    "check": "node tools/validate-site.mjs",
    "test": "node --test tests/*.test.mjs",
    "deploy": "sh tools/deploy-site.sh",
    "sync": "node tools/modularize-html.mjs . && node tools/validate-site.mjs",
    "refresh": "npm run sync && npm run build"
  }
}
